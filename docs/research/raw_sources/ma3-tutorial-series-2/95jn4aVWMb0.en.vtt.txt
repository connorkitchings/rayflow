foreign [Music] tutorial series in the past couple of videos we've talked about how to store presets and change the store mode you can find links to them in the description below in this video we're going to look at how to use filters filters are a way to store and call specific attributes while restricting other attributes to create a filter we need to create a filter pool you can find the filter pool by clicking on an open space and then in the pools tab click on filters now that we have a filter pool we can see that there is already a filter created labeled open this is just a default filter that does not restrict any attributes if we want to make a new filter right click on an open pool item and a filter edit window will pop up in this window we can see a full list of each feature group feature and attribute that apply to the available attributes from the fixtures that are patched if we click on the drop down arrow to the left of the feature group we will see additional features and attributes available within each feature group at the bottom of the window we can also see additional ways of selecting what fixtures this filter we are creating can be applied to let's create a filter that only uses the pan attribute I'll first label it pan and then press the select none button at the top right to deselect everything then I will click on the P for pan we can now see that the pan selection has highlighted the position feature group the pan tilt feature and finally the pan attribute with the selection whether I store or call a preset only the pan attribute will change values also while the pan filter is selected no other attributes can be affected let's click out of the filter edit window and create one more filter for tilt I'm going to use the exact same method as before but selecting t for tilt this time now that we have these filters created let's see how to use them I'm going to use my beam fixtures since I already have two position presets for them the first position preset has the beam fixtures Fanning out and tilted low the second position preset has the beam fixtures Fanning in and tilted High I want to create six new position presets that will stem from these two let's start with just the pan attributes I want two new position presets that only have pan values stored in them we will use the pan filter to only allow the programmer to store the attributes I have not filtered out which in this case is only the pan attribute I'll start by selecting the beam fan out low preset again now if I press the pan filter and then store the new position preset we can see below in the encoder bar that only the pan values were stored since the pan attribute is no longer highlighted in red let's label this beam fan out since the pan filter is still selected let's go ahead and press the beam fan in high preset you may have noticed when I clicked on the preset the fixtures did not tilt high like they normally would this is because the pan filter did not allow the Tilt information to be called since that information is filtered out from being called or stored I'll store this and label it beam fan in now that the pan values have been dissected from the two original presets let's select the Tilt filter and store those position presets since the Tilt value is still active from the beam fan out low preset I can store it as a new preset and then label it beam low [Music] I'll go ahead and create a beam High position preset using the Tilt value from the beam fan in high preset [Music] great now we have two pan only presets and two tilt only presets that we can now use independently of each other the only other presets I want to make are a beam fan out High and a beam fan in low before I can begin combining attributes I'll need to click on the open filter so that all attribute information can begin working together again this is an important thing to remember because if you continue trying to store presets of any type while a filter is on you may end up with presets that either only have part of the attribute information you want to store or none of it at all to make the beam fan out High position preset I'll select the beam fan out and beam High presets and then store it even though those attributes were already visibly displayed they were not active in the programmer shown by the pan and tilt boxes being highlighted in Red so I just have to recall that information to make the beam fan in low position preset I'll select the beam fan in and beam low presets and then store it now that we have seen how to use filters with position information the exact same methods can be used for all attribute types for instance you can split up color wheel information if a fixture has more than one color wheel you can split Global information from the Google rotation value and so on the possibilities are endless so use your creativity to boost the functionality of your show file the last thing to do is clear our selection and always remember to save our show file foreign