[Music] foreign [Music] hey guys welcome back to this Grand number 3 tutorial series today we're going to learn how to store sequences and assign them to executors sequences contain attribute values that can be stored and played back in the way of cues this allows you to create specific looks and have many features work together and change with the Press of one button let's start by opening a sequence pool and playbacks window after clicking on an empty space the sequence pool can be found under the data pools tab and the playbacks can be found under the common tab I'm going to quickly deselect rows 400 300 and 100 in the playbacks window settings so we can focus on the executors with fader control the first sequence I'm going to make is a dimmer control for my beam fixtures I'll start by selecting the beam group followed by setting their dimmer value to 100. just like storing new presets let's left-click and hold over a sequence pull item to store this as a new sequence using the swipey feature let's label this sequence beam dimmer now that our sequence is stored we need a way to control it this is where the executors in the playbacks window come in to assign the control of our new sequence to an Executor we will use the swipey feature again to select assign and then click on our first executor in the playbacks window let's clear our selection so we can deactivate the preset output and then take a look at our new executor even though the fader says it's at 100 percent the executor is not active yet until we either bring the fader to zero and then back up or simply press the Go Button we can now change the intensity of our beam fixtures with a fader sequences also have the ability to reference preset information making the creation of cues that much easier let's store a couple of positions and colors into a new sequence I'll select my first beam group again but this time click on the first position preset beam fan out low as well as the lime color preset instead of storing to the sequence pool and then assigning to an Executor I'm going to store directly to the next available executor since the swipey feature cannot be used with executors I'm just going to press the store button in the command control window and then click on the new executor once again let's clear our selection to deactivate the preset output and then press go on the new executor to activate the sequence to label this executor I'll go back into the command control window and press the assign button twice if we look at our command line pressing assign twice enters the Syntax for label now we can go back to our executor and click on it to type in a new label as we can see updating the name of an Executor also updated the name of the sequence to add more cues with the beam fixtures to the sequence all you have to do is select the beam group and then the presets you want for the second look once you have them selected you can store them to your sequence after choosing your store mode for this we just want to create a second cue so that is what we will select we'll clear our selection and then press go on the executor now every time we press the Go Button the sequence will scroll through the two cues that we've made we can add fade times to make a smoother transition between cues but that and all other sequence functions will be shown in a future video a big point that I want to make here is that the second method which used presets we already made embeds the sequence with preset information basically any changes you make to the presets you have stored into sequences will automatically update in their respective sequences however fixture group information will not update in sequences if fixtures are added or removed from the group that was used to make the sequence this is where recipes come in handy which we'll get to in the next video let's make sure our selection is clear and always remember to save our show file [Music]